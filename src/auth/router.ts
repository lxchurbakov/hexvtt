// export const router
    // post /session { email } -> null (sends code to their email)
    // post /session/exchane { email, code } -> jwt
    // get /session {} -> User | null - returns information about user
